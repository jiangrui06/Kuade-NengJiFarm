  修复与开发方案（按优先级排序）

  P0 — 立刻修复（功能明显不可用）

  1. 修复 order-coupon.html 核销 API 调用
  - confirmWriteOffCouponOrder() 方法需要调用 POST /api/activity-order/verify
  - 请求体：{ orderId: orderId, verifyCode: ... }
  - 当前只改了前端状态，刷新就丢

  2. 修复 order-dish-detail.html 退款按钮
  - 为两个退款按钮绑定 @click 事件
  - 先确认后端是否支持菜品订单退款（当前 API 文档中 DishOrderController 没有退款接口，需要先加后端支持，或者前端做提示）

  P1 — 重要功能完善

  3. 侧边栏增加「餐桌管理」入口
  - 在所有页面的侧边栏增加第 6 项：<li @click="navigateTo('table.html')">餐桌管理</li>
  - 统一侧边栏 6 个菜单：产品管理、订单管理、菜品管理、活动券管理、餐桌管理、用户管理

  4. 完善 order-coupon.html 退款功能
  - Review current refund logic — appears to call API but verify if it works end-to-end

  5. 菜品管理增加批量删除
  - 参考 product.html 的批量删除实现，加到 dish.html

  P2 — 新功能开发

  6. 员工核销管理页面（后端 API 就绪）
  - 新建 staff.html — 今日统计卡片、核销券列表、核销记录列表
  - 新建 staff-verify.html — 核销操作页面（输入订单号+核销码）
  - 侧边栏增加入口

  7. 厨房管理页面（后端 API 就绪）
  - 独立子系统，需要登录页面 kitchen-login.html
  - 主页面 kitchen.html — 订单列表、完成/取消菜品操作、今日统计

  8. 管理员角色选择
  - 用户新增/编辑页面接入 /api/back-user/roles/list
  - 角色下拉框从接口获取

  P3 — 代码清理

  9. 清理 order-dish.html 死代码
  - 移除约 600 行商品订单弹窗代码（productActionModal*、allProductOrders、loadProductOrders）
  - 购物车满减的英文注释改为中文

  10. 修复 order-coupon.html CSS 问题
  - last-child:border-bottom:none 改为用 CSS 类或 v-for 的 index 判断