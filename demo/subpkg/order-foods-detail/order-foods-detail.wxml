<view class="container">

  <!-- 商品图片 -->
  <view class="goods-image-container">
    <image class="goods-image" src="{{goods.image}}" mode="aspectFill" />
  </view>

  <!-- 商品信息 -->
  <view class="goods-info">
    <view class="price">¥{{goods.price}}</view>
    <view class="name">{{goods.name}}</view>
    <view class="desc">{{goods.desc || '新鲜食材，农场直供'}}</view>
    <view class="sales-info">
      <text class="sold">已售 {{goods.sold}}份</text>
      <text class="stock">库存 {{goods.stock}}份</text>
    </view>
  </view>

  <!-- 商品详情图片 -->
  <view class="detail-image">
    <image src="{{goods.image}}" mode="aspectFill" />
  </view>

  <!-- 底部操作栏 -->
  <view class="bottom-bar">
    <view class="cart-icon" bindtap="viewCart">
      <text class="cart-text">🛒</text>
      <block wx:if="cartCount > 0">
        <view class="cart-badge">{{cartCount}}</view>
      </block>
    </view>
    <view class="btn-container">
      <button class="add-cart-btn" bindtap="addToCart">加入购物车</button>
      <button class="buy-btn" bindtap="buyNow">立即购买</button>
    </view>
  </view>
</view>