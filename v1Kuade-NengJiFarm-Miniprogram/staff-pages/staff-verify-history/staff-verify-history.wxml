<view class="container">
  <!-- 筛选区域 -->
  <view class="filter-section">
    <!-- 搜索框 -->
    <view class="search-box">
      <image class="search-icon" src="https://img.icons8.com/ios-glyphs/30/999999/search.png" />
      <input
        class="search-input"
        type="text"
        placeholder="搜索核销码/用户名"
        value="{{searchKeyword}}"
        bindinput="onSearchInput"
      />
    </view>

    <!-- 筛选标签 -->
    <view class="filter-tags">
      <text
        class="filter-tag {{filterType === 'all' ? 'active' : ''}}"
        data-type="all"
        bindtap="onFilterChange"
      >全部</text>
      <text
        class="filter-tag {{filterType === 'pick' ? 'active' : ''}}"
        data-type="pick"
        bindtap="onFilterChange"
      >采摘券</text>
      <text
        class="filter-tag {{filterType === 'activity' ? 'active' : ''}}"
        data-type="activity"
        bindtap="onFilterChange"
      >活动券</text>
      <text class="filter-tag date-tag" bindtap="selectDateRange">
        📅 {{dateRange.startDate || '选择日期'}}
      </text>
    </view>
  </view>

  <!-- 加载中 -->
  <view wx:if="{{loading && historyList.length === 0}}" class="loading">
    <view class="loading-ring"></view>
    <text class="loading-txt">加载中...</text>
  </view>

  <!-- 核销记录列表 -->
  <view wx:if="{{!loading || historyList.length > 0}}" class="history-section">
    <!-- 统计信息 -->
    <view class="stats-bar">
      <text class="stats-text">共 {{total}} 条记录</text>
    </view>

    <!-- 列表 -->
    <view class="history-list">
      <view
        class="history-item"
        wx:for="{{historyList}}"
        wx:key="id"
        bindtap="viewDetail"
        data-item="{{item}}"
      >
        <view class="history-left">
          <text class="history-type {{item.voucherType === 'pick' ? 'pick-tag' : 'activity-tag'}}">
            {{item.typeName}}
          </text>
          <view class="history-detail">
            <text class="history-user">{{item.userName}}</text>
            <text class="history-content">{{item.content}}</text>
          </view>
        </view>
        <view class="history-right">
          <text class="history-time">{{item.verifyTimeFormatted}}</text>
          <text class="history-status success">已核销</text>
        </view>
      </view>
    </view>

    <!-- 空状态 -->
    <view wx:if="{{historyList.length === 0 && !loading}}" class="empty-state">
      <image class="empty-icon" src="https://img.icons8.com/ios-glyphs/100/cccccc/inbox.png" />
      <text class="empty-text">暂无核销记录</text>
      <text class="empty-subtext">核销后的记录会显示在这里</text>
    </view>

    <!-- 加载更多 -->
    <view wx:if="{{hasMore && historyList.length > 0}}" class="load-more">
      <text class="load-more-text">上拉加载更多</text>
    </view>

    <!-- 没有更多数据 -->
    <view wx:if="{{!hasMore && historyList.length > 0}}" class="no-more">
      <text class="no-more-text">没有更多数据了</text>
    </view>
  </view>
</view>
